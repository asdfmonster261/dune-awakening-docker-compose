# Secrets — never commit
.env
.env.local
dune-setup/certs/
dune-setup/orchestrator/serviceaccount/

# Rendered configs (have credentials substituted in)
dune-setup/gateway-override.ini
dune-setup/orchestrator/world.yaml

# Funcom-provided artifacts (download via setup.sh / Steam — not redistributable)
server/

# Reverse-engineering notes — local only
ANALYSIS.md
research/

# Compiled binaries
dune-setup/orchestrator/dune-orchestrator

# update.sh artifacts
.updates/
.last-update
docker-compose.yml.bak

# mini-UE4SS deployment dir (built artifacts + per-game config); the .so is
# rebuilt from the mini-UE4SS repo, the config files live in
# mini-UE4SS/profiles/dune-1979201/. Don't track any of it here.
mini-ue4ss/

# Editor / OS junk
.DS_Store
*.swp
*~
.idea/
.vscode/
